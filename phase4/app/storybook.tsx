// ABOUTME: Storybook route — renders component stories in isolation
// ABOUTME: Only accessible in development builds

import StorybookUIRoot from '../.rnstorybook';

export default function StorybookScreen() {
  return <StorybookUIRoot />;
}
